apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csr-auto-approve.fullname" . }}-test-connection"
  labels:
    {{- include "csr-auto-approve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csr-auto-approve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
