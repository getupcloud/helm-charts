apiVersion: v1
kind: Service
metadata:
  name: {{ include "csr-auto-approve.fullname" . }}
  labels:
    {{- include "csr-auto-approve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csr-auto-approve.selectorLabels" . | nindent 4 }}
